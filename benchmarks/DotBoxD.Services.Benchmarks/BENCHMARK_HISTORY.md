# DotBoxD Services Benchmark History

All results below are local stopwatch probes on this machine, run in Release mode.

## Commands

```text
dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --filter "*RpcTelemetryBenchmarks*" --job short --warmupCount 3 --iterationCount 8
dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-peer-proxy-cache
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-stream-connection-receive-tracking
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-stream-connection-pending-receive
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-stream-connection-pending-receive-core
DOTNET_TieredCompilation=0 DOTBOXD_TRANSPORT_RECEIVE_BATCH_SIZE=16 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-transport-receive-lookahead
dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-transport-large-frame-receive
dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-transport-idle-receive-footprint
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-transport-connection-construction
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-tcp-send-gate-contention
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-peer-start-guard
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-peer-frame-dispatch
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-request-name-cache
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-constructor-replay
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-pending-timeout-scheduler
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-pending-request-removal
DOTNET_TieredCompilation=0 DOTNET_TieredPGO=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-pending-response-wrapper-cleanup
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-pooled-buffer-writer-cache
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-pending-owned-frame-send
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-frame-read-timeout-source-lifetime
DOTNET_ROLL_FORWARD=Major dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --probe-generated-metadata-parameters
DOTNET_TieredCompilation=0 dotnet run -c Release --project benchmarks/DotBoxD.Kernels.Benchmarks -p:UseSharedCompilation=false -- --profile-ipc namedpipe 20000 --low-alloc --no-timeout
dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks -p:UseSharedCompilation=false -- --filter "*PeerRoundTripBenchmarks.MovePlayerAsync*" --job Short --warmupCount 1 --iterationCount 3
DOTNET_ROLL_FORWARD=Major dotnet run -c Release --project benchmarks/DotBoxD.Services.Benchmarks/DotBoxD.Services.Benchmarks.csproj -- --filter "*StreamedArgumentProxyBenchmarks*" --job short --warmupCount 1 --iterationCount 3
```

## Ledger

| Change | Commit | Probe | Result |
| --- | --- | --- | --- |
| Skip consumed pending-response cleanup | `d649b7821` | `--probe-pending-response-wrapper-cleanup` | Terminal producers already remove correlation before signaling response, protocol failure, caller cancellation, timeout, or connection failure. The four backpressured wrappers now skip the guaranteed second locked lookup after consuming or taking ownership of that signal, while every unconsumed/send-failure path retains removal, result disposal, pooled abandonment, slot release, and recycling. Ten alternating fresh Tiering/PGO-disabled process pairs pinned to CPU 3 measured pooled unary `694.0→688.5 ns`, pooled no-response `656.4→643.4 ns`, Task unary `691.6→685.3 ns`, and general Task `818.3→809.0 ns`; each candidate median is 5.5-13.0 ns lower with 7/10 wins. The unchanged locked-miss lane measures about 9 ns, so the movement is consistent with removing one miss but does not receive an exact end-to-end timing claim. Synchronous controls are noise-flat. Allocations stay byte-identical in every process at `168/144/312/608 B/op`, with zero for controls. The probe validates 2,080,004 responses/source generations, exact result and message-ID checksums, writer disposal before response delivery, and four `MaxPendingRequests=1` follow-ups per pending harness. Focused and full Services tests cover response-before-send, send-failure losers, source reuse, timeout/cancellation, and stream ownership. Ordinary 20,000-call allocation controls remain within established ranges (`283.7/270.8 B` named-pipe Add/Echo; `1.0/0.0 B` TCP). |
| Cached outbound RPC request-name UTF-8 | `7d7548550` | `--probe-request-name-cache` | Registered outbound names now return their immutable UTF-8 bytes to the normal MessagePack writer instead of re-encoding on every envelope. Across five fresh Tiering-disabled processes, one-million-operation medians improve 218.3→175.9 ns (19.4%) for 64 two-byte characters, 372.3→270.8 ns (27.3%) for 128 two-byte characters, and 359.3→202.5 ns (43.6%) for 64 astral characters, all at 0.0 B/op measured. The short registered control is effectively flat at 110.9→108.1 ns; over-boundary 257-byte ASCII and 258-byte multibyte controls are flat at 377.2→376.5 and 371.3→370.5 ns. Length checksums are unchanged, canonical/OldSpec byte tests pin wire compatibility, real named-pipe/TCP allocation controls retain their prior totals, and no end-to-end RPC latency improvement is claimed. |
| Bounded pooled pending Stream receive completions | this commit | `--probe-stream-connection-pending-receive-core`, `--probe-stream-connection-pending-receive`, `--probe-stream-connection-receive-tracking`, `--probe-transport-receive-lookahead`, and `--profile-ipc namedpipe 20000 --low-alloc --no-timeout` | A reusable `IValueTaskSource<RpcFrame>` is rented only when a Stream/named-pipe read genuinely suspends and is returned only after both producer completion and legal consumer observation; one atomic hot slot plus 16 bounded overflow entries prevent unbounded retention. Five alternating Tiering-disabled forced-pending pairs remove the native completion allocation exactly from 160 B/frame to effectively zero and reduce the legacy payload lane 352→192 B/frame while improving finite/infinite medians 742.9→688.0 and 593.6→537.4 ns/frame. Five real named-pipe pairs reproduce 304.1→144.1 B/frame; three batch-16 pairs reproduce 19→9 B/frame. Three 20,000-call named-pipe profiles reduce Add/Echo medians exactly 320 B/call, from 609.0/608.1 to 289.0/288.1 B/call. The five-pair synchronous control remains exactly 32 B/frame and improves 256.8→245.9 and 111.6→107.1 ns; untouched TCP controls remain 160.1 B/frame for gated receives and 10 B/frame for batched pending receives. OS latency is scheduler-noisy and the IPC profile has no per-method timer, so neither receives a latency claim. Lifecycle tests cover producer/consumer races, early/stale/double consumption, inline continuations, bounded recycling and state clearing; Stream tests additionally pin `ExecutionContext` flow across repeated suspensions, timeout/cancellation mapping and rearming, EOF/lookahead behavior, cleanup, reuse, and retained-reference release. |
| Bounded pooled Stream receive population | this commit | `--probe-transport-idle-receive-footprint[-direct]`, `--probe-stream-connection-receive-tracking[-post-capacity]`, `--probe-stream-connection-pending-receive[-core]`, `--probe-transport-receive-lookahead`, and named-pipe/TCP IPC profiles | The retained Stream source cache was bounded, but 1,000 simultaneous cold receives still created 1,000 custom sources. Three fresh-process sets measure direct allocation at `1,187,904→1,132,936 B` and explicit `AsTask` at `1,284,088→1,135,672 B`; relative to the pre-pooling task-backed medians, the final overhead is only about `1.2/3.8 B/connection` instead of `56.1/152.2 B/connection`. Reusable source creation is now lifetime-capped at 17. A default-false Stream-only preflight selects the baseline-shaped scalar task loop only while the pool is exhausted, clears after observing returned sources, and re-arms when a pending rent drains the pool; stale races use a safe transferred-state fallback and heal the hint after a miss. Fresh and post-capacity controls retain exact `2,800,032 B` first-EOF and `32 B/frame` synchronous totals, with five-pair timings inside 0.7% for the repeated infinite lane after saturation. Five recurring forced-pending pairs remain exactly `144/432 B` native finite/infinite and `19,200,760 B` legacy; batch-16 remains 9 B/frame and real named-pipe/TCP RPC allocations remain at their prior ranges. Collective capacity-plus-one tests cover lookahead timeout, exact-read cancellation, EOF progress, caller `ExecutionContext`, frame ownership, and recovery while pending sources occupy the population. |
| Bounded pooled pending TCP receive completions | this commit | `--probe-stream-connection-pending-receive`, `--probe-transport-receive-lookahead`, `--probe-transport-large-frame-receive`, `--probe-transport-idle-receive-footprint`, `--probe-transport-connection-construction`, `--profile-ipc tcp 20000 --low-alloc --no-timeout`, plus one-off TCP legacy/cold harnesses | TCP now shares the hardened reusable completion lifecycle but keeps a specialized driver. Five paired gated processes remove exactly 160 B per pending native frame (`3,201,136→1,136 B` over 20,000 frames, `160.1→0.1 B/frame`). Five batch-16 pairs remove exactly 8,000,000 B over 800,000 frames (`10.0→effectively 0 B/frame`) while the buffered control stays exactly 1,040 B. Five alternating 20,000-call loopback RPC pairs reduce Add/Echo allocation medians from `321.0/320.1` to `0.9/0.9 B/call`, corroborating the roughly 320-byte saving from two pending receives per round trip. Legacy `ReceiveValueAsync` and `ReceiveAsync` fall exactly `352.1→192.1` and `328.1→168.1 B/frame`; three 256 KiB pairs remove a median-exact 160 B/frame. Construction/disposal remain exactly 272/0 B. Reusable-source creation is capped at 17; saturated calls take a baseline-shaped 80-byte task-backed loop, bounding 1,000-connection cold overhead to about 2.1 B/connection direct and 2.896 B/connection through `AsTask` in the final post-composition set. Stream-specific production files are restored byte-for-byte to the prior commit, with exact first-EOF/synchronous/forced controls. Gated timing moves in opposite directions and receives no latency claim; buffered/batched/large controls stay within the regression guard. The IPC profiler does not time individual methods, so the full-RPC result is also allocation-only. Collective capacity-plus-one tests cover body timeout, linked cancellation, producer/caller context separation, cleanup, and recovery. |
| Pooled native receive-frame ownership | this commit | `--probe-stream-connection-pending-receive`, `--probe-transport-receive-lookahead`, `--probe-transport-large-frame-receive`, `--probe-stream-connection-receive-tracking`, and named-pipe IPC profiles | Native `RpcFrame` receives now fill the already pooled writer owner instead of allocating a 32-byte `Payload` object around the same rented array. Buffered batches fall exactly from 32.0 to effectively 0 B/frame for both named pipes and TCP; one-pending-in-16 batches fall 51.0→19.0 and 42.0→10.0 B/frame. Forced two-pend and real one-pend named-pipe/TCP frames remove the full 32 B (`192→160`, `336→304`, and `192→160` B/frame) because the legacy/native mode occupies existing connection padding rather than the pending state-machine box. Three 20,000-call named-pipe profiles reproduce about 64 B/call saved in the queue-free pooled-client profile (`672.2-673.0/672.1-673.7→608.1-609.8/608.1-608.9` B for Add/Echo) and about 32 B/call with the default bounded queue (`928.4-930.1/936.1→896.6-896.7/904.1-905.7` B). Bounded requests and retained task/streaming responses materialize the old 32-byte `Payload` owner before long-lived handoff; the writer overflow cache is capped at 16. Eight alternating legacy controls remain allocation-identical at 32 B synchronously and 352 B pending; finite synchronous/pending medians move 3.14%/1.55% and infinite synchronous timing is flat, all inside the 5% guard, so this is allocation-only. Eight unpinned 256 KiB counted-pipe/TCP controls also remain inside the guard (`80.95→82.95` and `34.65→33.80` us/frame); process-wide allocation is noisy for this OS lane, while the controlled probes establish the exact owner reduction and the pool refuses to retain buffers over 4 KiB. Connection construction remains exactly 280 B for Stream/named-pipe and 272 B for TCP. |
| Explicit named-pipe OS buffers (rejected) | no production change | experimental 16/32/64 KiB constructor-buffer matrix: 16-frame bursts, bidirectional 256 KiB frames, and 20,000-call unary RPC | The platform default passes 0/0; Linux measured 212,992-byte socket buffers, while requested 16/32/64 KiB produced only 32,768/65,536/131,072 bytes. Six alternating 256 KiB server-send blocks regressed counted medians from 79.3 us and 2.976 reads/frame to 292.1 us/9.989 reads at 16 KiB and 184.6 us/5.984 reads at 32 KiB. A focused ten-pair 64 KiB run was noise-flat (5/10 latency wins, about -1.25% paired median), but reads regressed every run from 2.978 to 2.999/frame and allocation did not improve. Ten default/64 KiB unary pairs favored the candidate only 2/10 Add and 4/10 Echo runs, with allocations flat and paired latency medians directionally 3.34%/2.88% worse. Explicit cross-platform defaults were rejected because they lower Unix capacity and alter Windows per-instance quota/backpressure without a measured win. |
| Listener-free RPC request telemetry | this commit | `RpcTelemetryBenchmarks.SuccessfulRequestWithoutListeners` and `PeerRoundTripBenchmarks.MovePlayerAsync` | The focused scope benchmark improved from 55.49 ns / 64 B to 0.54 ns / 0 B by keeping only a one-reference value scope and creating detailed request state when tracing or duration metrics are enabled. Enabled duration tags, activities, and timeout counters remain covered by tests. The end-to-end control corroborated exactly 64 B/request saved in both profiles (640 B to 576 B and 320 B to 256 B); its asynchronous latency moved in opposite directions between runs, so only the isolated latency and end-to-end allocation improvements are claimed. |
| Root RPC proxy cache | `7126981` | `--probe-peer-proxy-cache` | Repeated locked proxy creation measured 33.5 ms / 32,000,040 B for 1,000,000 calls; cached `RpcPeer.Get<IGameService>` measured 36.9 ms / 40 B. This is an allocation-only win for the small root proxy in the probe, with registry-stamp invalidation preserving replacement registration behavior. |
| Owned stream receive tracking | `4ec4439` | `--probe-stream-connection-receive-tracking` | Owned empty-stream receives with legacy active-receive tracking simulated around the current body measured 124.2 ms / 72,001,584 B for 1,000,000 calls; current owned receives measured 113.8 ms / 72,000,040 B. Non-owned streams still track active receives for blocked-read shutdown. |
| Generated Get extension proxy cache | this commit | `--probe-peer-proxy-cache` | Repeated generated `GetGameService` calls used to directly construct `GameServiceProxy`, measuring 8.6 ms / 32,000,040 B for 1,000,000 calls. Routing generated extensions through cached `RpcPeer.Get<IGameService>` measured 37.9 ms / 40 B, so this is an allocation-only tradeoff and preserves shared root proxy identity. |
| Unary frame-channel ownership | this commit | `PeerRoundTripBenchmarks.MovePlayerAsync` | Before this fix, the default profile row failed before measurement with `ObjectDisposedException: PooledBufferWriter` while `EndToEndLowAllocationProfile=true` still completed. After removing the post-transfer dispose, the same short BDN command completed both rows: default profile 3.769 us / 424 B, low-allocation profile 5.180 us / 104 B. This proves the benchmark blocker is gone; it is not a latency-improvement claim. |
| Generated single-stream attachment path | this commit | `StreamedArgumentProxyBenchmarks` | Generated proxies now pass one stream attachment through the `IRpcInvoker` single-stream overload and the runtime stores it in `RpcStreamAttachmentSet` without allocating a one-element attachment array. The short BenchmarkDotNet run measured `SingleStreamUpload` at 7.556 ns / 40 B/op and the two-stream array control `TwoStreamUpload` at 17.831 ns / 120 B/op. The two-stream path still needs an attachment array, so this step claims the single-stream allocation reduction only. |
| Generated zero-parameter metadata arrays | this commit | `--probe-generated-metadata-parameters` | Generated service metadata now emits `Array.Empty<GeneratedParameter>()` for methods with no metadata parameters instead of allocating a zero-length array in the generated static initializer. The 1M metadata-array probe measured the legacy zero-parameter allocation at 24,000,040 B and the current zero-parameter path at 40 B. The one-parameter control still allocates its metadata array. |
| Published RPC peer startup fast path | this commit | `--probe-peer-start-guard` | Ten million repeated healthy `Start()` checks improve from 68.9-71.8 ms (6.9-7.2 ns/op) to 8.2-8.3 ms (0.8 ns/op), with zero allocations before and after. A separate completion publication prevents callers from bypassing a partially completed first start; disposed and remotely closed peers retain the locked exception path. |
| Cached constructor replay metadata and indexed comparison | this commit | `--probe-constructor-replay` | Stable exact/base-declared constructor DTOs fall from 256 to 128 B per serialization. Four-process medians improve from 145.5 to 103.0 ns (-29.2%) and 145.4 to 116.6 ns (-19.8%). The cache stores reflection metadata only: constructor replay and comparison still run for every value, and runtime type mismatches retain dictionary resolution. |
| Hot typed constructor replay validation | this commit | `--probe-constructor-replay` | A visible, exact-declared simple DTO publishes a typed constructor/getter validator only after 8,192 successful reflective replays. Fifteen alternating Tiering-disabled processes improve the one-million-call median from 105.4 to 45.1 ns and reduce allocation exactly 128→24 B/call; .NET 9 can elide even the replay object in the allocation regression. With the process expression compiler warm, an additional single-threaded one-int DTO activates in a median 205.2 us / 7,648 B; its next 8,192 calls improve 135.4→51.8 ns/call and recover that shape's per-type cost before 16,384 total uses. This excludes first-validator compiler/JIT startup, concurrent approximate-admission delay, and other shape costs. First use of that additional DTO remains allocation-identical at 30,616 B after process/serializer warm-up; both pre-admission batches remain 128 B/call and move in opposite latency directions within process noise. Runtime-via-base, non-visible, and dynamic-code-disabled shapes remain reflective; complex and settable DTOs retain their prior paths. NativeAOT smoke now crosses the threshold with a hand-written formatter. |
| Lock-free later-deadline timeout scheduling | this commit | `--probe-pending-timeout-scheduler` | Ten million no-op later-deadline schedules improve from 67.2-70.1 ms (6.7-7.0 ns/op) to 13.9-14.8 ms (1.4-1.5 ns/op), with zero allocations before and after. The earliest hint is published with volatile writes, the locked path revalidates before changing the timer, and a paused timeout-scan regression proves a concurrent later deadline is retained. End-to-end finite-unary timings moved directionally but remain process-noisy, so only the isolated scheduler latency is claimed. |
| Single-traversal matched response removal | this commit | `--probe-pending-request-removal` | `PendingRequests.TryTake` now uses `Dictionary.Remove(key, out value)` to retrieve and remove a matched response in one traversal under the existing lock. Across ten fresh Tiering-disabled processes, one million prepopulated matched removals improve from an 8.4 to 3.3 ns/op median (60.7%), with zero measured allocation before and after and all ten processes favoring the single traversal. Ten paired full finite/infinite unary processes moved similarly (627.8→615.3 and 601.2→591.5 ns), so no end-to-end latency improvement is claimed. Identity-sensitive cancellation and timeout removals intentionally retain their guarded two-step check. |
| Frame-scoped receive idle timeout | this commit | `--probe-stream-connection-pending-receive`, `--probe-stream-connection-receive-tracking`, and named-pipe IPC profile | Directly awaiting stream reads inside the existing exact-read state machine removes the two finite-timeout wrapper boxes: 100,000 forced-pending frames fall from 79,200,480 B (792 B/frame) to 47,200,480 B (472 B/frame), exactly matching the infinite-timeout control. Ten alternating process pairs improve the forced-pending finite median 20.4% (1,343.8 to 1,069.1 ns/frame); the paired infinite control is flat (900.5 to 896.6 ns). Keeping one timer armed across prefix and body reduces a typical frame from four timer operations to three; the synchronous finite median improves from 372.1 to 284.5 ns (23.5%) while its infinite median remains flat. Three 20,000-call named-pipe profiles fall from 1,264.2-1,264.3 to 944.2-944.3 B/call for Add and 1,264.0-1,264.1 to 944.0-944.1 B/call for Echo. |
| Direct built-in raw sends | this commit | `--probe-peer-frame-send` | Built-in Stream/named-pipe and TCP channels already validate and serialize every send, so raw peer sends now bypass the redundant peer semaphore through the same internal capability as owned frames. Five TieredCompilation-disabled processes reduce median peer overhead from 44.0 to 12.4 ns for default tokens and from 42.5 to 12.2 ns for reusable live tokens (71.8% / 71.3%), with zero allocations throughout. Closed, pre-canceled, malformed, and mixed frame/raw cases remain enforced by the peer/transport boundary; third-party channels retain peer serialization and cancellable waiters. |
| Direct inbound frame dispatch | this commit | `--probe-peer-frame-dispatch` | Replacing each peer's nine-delegate dictionary with direct enum dispatch removes exactly 1,576 B per frame processor (1,624→48 B) and improves the five-process construction median from 144.8 to 12.4 ns. Allocation-free Cancel dispatch improves from 40.3 to 16.0 ns (60.3%); valid unmatched Response handling, including envelope decoding and pending-table lookup, improves from 170.6 to 133.9 ns (21.5%). Directly returning the selected handler's `ValueTask` also avoids another wrapper for pending requests. Malformed/unknown frames and all nine defined message kinds retain focused coverage. |
| Terminal frame-timeout source disposal | this commit | `--probe-frame-read-timeout-source-lifetime` | Wrapper disposal now linearizes with only slow source creation/replacement, so a partial read cannot rearm after connection disposal and retain a new timer. Ten alternating process pairs keep the cached Start/Cancel median flat at 81.1 to 79.5 ns and 0 B/op; source allocation remains exactly 40 B. The allocation-free wrapper monitor adds 6.4 ns to one-time Dispose (3.4 to 9.8 ns median) and avoids the 32 B/source cost of a separate gate. Post-disposal Start changes from recreating 144 B unlinked or 192 B linked state to throwing `ObjectDisposedException`; deterministic connection coverage proves no second read or retained source. |
| One-state-machine Stream/TCP frame receive | this commit | `--probe-stream-connection-pending-receive`, `--probe-stream-connection-receive-tracking`, and `--profile-ipc namedpipe 20000 --low-alloc` | Each transport now directly owns one prefix/body receive loop, replacing the outer receive plus two exact-read async boxes with one state machine while sharing synchronous EOF/timeout primitives. A single remaining-byte counter keeps the pending box compact: the forced two-pend lane falls exactly from 472 to 192 B/frame (59.3%) while retaining two reads. Five alternating processes improve finite/infinite medians from 1,097.0/930.9 to 776.3/620.1 ns (29.2%/33.4%). Real named-pipe allocation falls from 472.1 to 336.1 B/frame and TCP from 336.1 to 192.1 B/frame; OS latency remains too noisy to claim. The synchronous control stays at 32 B/frame and improves from 291.5/156.5 to 260.5/120.5 ns (10.6%/23.0%). Full named-pipe unary Add/Echo calls fall by about 272 B each, from 944.2-944.3/944.0-944.1 to 672.2/672.0-672.1 B/call. Direct ownership also preserves the original connection-construction allocation; a composed reader (+56 B/connection) and generic context (3.5-9.2% slower) were measured and rejected. |
| Adaptive TCP/named-pipe receive lookahead | this commit | `--probe-transport-receive-lookahead`, `--probe-transport-large-frame-receive`, `--probe-transport-idle-receive-footprint`, `--probe-stream-connection-pending-receive`, and `DotBoxD.LookaheadCapacityProbe` | Owned named-pipe and TCP connections start with an exact four-byte prefix and use a 16 KiB pooled body window only when that prefix completed synchronously and the frame fits. Five alternating-process medians improve prebuffered 16-frame batches from 737.2 to 384.4 ns/frame for direct named pipes (47.9%) and 688.6 to 286.7 ns/frame for TCP (58.4%). With the first receive genuinely pending before every write, named pipes improve from 2,139.2 to 1,605.2 ns/frame (25.0%) and TCP from 1,864.4 to 1,428.6 ns/frame (23.4%). Counted named-pipe reads fall from 2.000 to 0.125/frame buffered and 0.250/frame pending; allocations remain exactly 32.0/51.0 B per named-pipe frame and 32.0/42.0 B per TCP frame. A missed exact-body lookahead backs off for 255 frames, while real carry stays enabled. One thousand pending idle connections per OS transport retain zero lookahead windows. Batch-1 and forced-pending exact controls remain allocation-identical; independent process sets moved in both directions, so no control-latency improvement is claimed. Independent 256 KiB process sets remain inside the 5% regression guard with flat median TCP allocation; their latency is likewise treated as noisy. The standalone raw-reader 32/64 KiB regressions informed the selected bound but do not model the final adaptive path, which reads larger frames directly. |
| CTS-free terminal TCP send wakeup | this commit | `--probe-tcp-send-gate-contention`, `--probe-transport-connection-construction`, and `--probe-stream-connection-send-gate` | TCP disposal now publishes closure and injects one terminal semaphore permit instead of retaining a per-connection disposal CTS. Waiters acquire, observe closure, and cascade the permit; mixed cancellation/disposal and future sends are pinned by real-backpressure tests. Five-process contended enqueue medians improve from 665.4 to 160.1 ns/waiter with default tokens and from 621.8 to 254.7 ns/waiter with live reusable tokens. Synchronous enqueue allocation falls exactly from 696 to 224 B/waiter and from 1,000 to 552 B/waiter, respectively; teardown completion is outside this measurement. TCP construction falls from 312 to 272 B/connection; named-pipe construction also falls from 296 to 280 B after sharing the gate helper and close monitor. Disposing 256 distinct live idle TCP connections remains exactly 0 B/op. The existing uncontended Stream control improves from 81.0/82.4 to 66.8/67.8 ns for default/live tokens, with 0 B/op throughout. |
| Retained hot framing buffers | this commit | `--probe-pooled-buffer-writer-cache` and `--probe-peer-frame-send` | Internal writer-cache returns now retain a <=4 KiB byte array in the same-thread slot or one atomic global handoff, while locked overflow entries return arrays immediately. Across ten alternating Tiering-disabled pairs, 5,000,000 same-thread 256-byte leases improve from a 53.2 to 18.3 ns median (65.6%), and 1,000,000 forced cross-thread leases improve from 394.7 to 286.3 ns (27.5%); the candidate wins all ten cross-thread pairs. Same-thread allocation remains zero. The measured cross-thread loop has the same fixed 672-B whole-process total (0.000672 B/lease) in both versions. First internal rent with `ArrayPool` prewarmed remains exactly 120 B, public construction remains 56 B/op, and same/cross-thread 8 KiB fallbacks are flat. Three final, non-alternating production-frame runs directionally corroborate the hot-path effect at 116.2-119.2 ns/direct send versus the prior 147.4-151.8 ns, still at 0 B/op. Focused tests pin the retention bound, max-written reset, and full distinct cross-thread writer identity set. |
| One-state-machine owned transport sends | this commit | `--probe-pending-owned-frame-send`, `--probe-tcp-send-gate-contention`, and ordinary named-pipe IPC | `StreamConnection` and TCP owned-frame sends now perform the existing raw validation/gate/write sequence and final writer release in one focused state machine instead of awaiting a second raw-send state machine. Ten alternating Tiering-disabled pairs reduce forced-pending Stream allocation exactly 280→160 B/send (42.9%) and its median 313.6→192.7 ns (38.5%); synchronous owned sends improve 119.1→87.3 ns at 0 B. The pending raw control remains exactly 152 B and its timing is noisy; the median difference-in-differences moves 88.8→-5.3 ns, consistent with removing the redundant state machine. Ten real TCP backpressure pairs independently remove exactly 120 B/owned waiter: default tokens fall 352→232 B and 309.3→273.0 ns, live tokens 600→480 B and 318.7→247.5 ns. Raw TCP remains exactly 224/552 B and receives no latency claim. Three 20,000-call ordinary named-pipe pairs remain allocation-flat (Add 672.2-673.8→672.2-673.9 B/call; Echo 672.1→672.0-672.9) because their small writes normally complete synchronously. Named pipes inherit the Stream helper; TCP mirrors it. Tests pin validation/ordering, writer lifetime through pending gate/write/flush, cancellation, connection disposal, faults, completion-driven release without an awaiting caller, gate reuse, and actual socket backpressure. |
| Direct owned streaming sends | this commit | `--probe-pending-owned-frame-send` | The streaming frame adapter now validates once and directly returns the owned transport's `ValueTask` instead of wrapping every send in another async state machine; the memory-only compatibility path retains its disposal wrapper. Ten alternating Tiering-disabled pairs reduce forced-pending streaming sends from a 339.5 to 201.1 ns median (40.8%) and exactly 312→160 B/send (48.7%), with all ten pairs improving. Synchronous streaming sends improve 117.5→96.2 ns at 0 B. The direct pending-owned control remains 160 B and is flat at 194.9→193.5 ns. Paired wrapper overhead falls from a 148.1 to 8.1 ns median, while its pending-minus-synchronous difference falls 120.1→-1.7 ns, consistent with removing the wrapper state machine. Ownership tests cover validation, synchronous and pending delegate failure/cancellation, lifetime through completion, and the memory fallback's success/failure/cancellation paths. |

Versioning note for the single-stream invoker overloads: the `RpcStreamAttachment` overloads on
`IRpcInvoker` are public generated-proxy ABI so generated proxies can make the allocation-free
single-attachment call across assembly boundaries. Their default interface implementations keep
custom invokers source-compatible by forwarding through the existing array overloads; the built-in
`RpcPeer`/outbound invoker override them for the allocation-free runtime path.
