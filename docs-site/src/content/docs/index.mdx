---
title: DotBoxD
description: >-
  DotBoxD is a contract-first plugin system for .NET hosts: one C# interface becomes typed RPC
  services, server-side event pipelines, and sandboxed pushdown batches — all generated at compile
  time, with no runtime reflection on the hot path.
template: splash
editUrl: false
hero:
  title: DotBoxD
  tagline: >-
    Let plugins extend your .NET host — safely. One C# interface becomes typed RPC, server-side
    filtered events, and plugin-shipped batch operations, generated at compile time. Untrusted
    plugin logic runs in a validated, fuel-metered sandbox — never loaded C#/IL.
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
    - text: What is DotBoxD?
      link: /overview/
      variant: minimal
      icon: open-book
    - text: GitHub
      link: https://github.com/JKamsker/DotBoxD
      variant: minimal
      icon: github
---

import { Card, CardGrid, LinkCard, Aside } from '@astrojs/starlight/components';

## What is DotBoxD?

**A complete plugin system for .NET hosts.** .NET can't safely isolate arbitrary code in-process,
so plugins live in a sidecar process behind IPC — safe, but every interaction costs a round-trip,
and the host vendor is forced to design event-filter APIs blind. DotBoxD ships that whole
architecture from one C# contract (transports, typed RPC, event pipelines) and dissolves both
costs: plugin-authored logic — filters, projections, batches — runs *inside the host* as a
validated, fuel-metered sandbox. The sandbox executes restricted IR (an intermediate
representation), never loaded C#/IL.

[Why DotBoxD? The dilemma, in three diagrams ›](/why-dotboxd/)

## One contract, three modes

You write one C# interface. DotBoxD delivers it three ways — pick by call shape:

<CardGrid stagger>
  <Card title="1 · Call the host — Services (RPC)" icon="rocket">
    The host implements the contract; clients call it through a generated typed proxy. Proxy and
    implementation come from the same interface, so they cannot drift — and it is all generated at
    compile time.

    [Tutorial: your first service ›](/tutorials/first-service/)
  </Card>
  <Card title="2 · React to the host — event pipelines" icon="seti:shell">
    A plugin subscribes with an ordinary `Where`/`Select` chain. The filter runs *inside the host*
    as sandboxed IR, so only matching, projected data ever crosses the wire — zero round-trips.

    [Tutorial: event pipelines ›](/tutorials/event-pipeline-runlocal/)
  </Card>
  <Card title="3 · Extend the host — Pushdown" icon="seti:pipeline">
    A plugin ships its *own* batch operation that runs **server-side** over the host's existing
    bindings, collapsing N round-trips into one validated submission — without recompiling the
    host.

    [Tutorial: pushdown server extension ›](/tutorials/pushdown-server-extension/)
  </Card>
</CardGrid>

Modes 2 and 3 are powered by the same engine — the [kernel sandbox](/concepts/kernels/): validated,
capability-gated, fuel-metered IR. Mode 1 is a trusted, hand-written host implementation.

## Install

```bash
# Full net10.0 stack — Services, the kernel sandbox, and Pushdown:
dotnet add package DotBoxD --prerelease

# Unity / netstandard2.1 service bundle:
dotnet add package DotBoxD.Services.All --prerelease
```

The Services and channel libraries target `netstandard2.1`. Unity/IL2CPP consumers must additionally
use generated/static MessagePack formatters, root the generated registry, and validate their build.
The Kernels and Pushdown stack targets `net10.0`.

## Security in one line

<Aside type="caution" title="Safe mode is the real boundary">
  A kernel is restricted IR (intermediate representation) that is validated, capability-gated,
  fuel/quota-metered, and (for compiled mode) verified before it runs — consumers never supply C#,
  IL, or arbitrary host calls. Trusted-plugin mode (`AssemblyLoadContext`) is **not** a sandbox.
  Read [Sandbox caveats](/security/sandbox-caveats/) before deploying.
</Aside>

## Find your path

<CardGrid>
  <LinkCard
    title="Getting started"
    href="/getting-started/"
    description="Install, get a first end-to-end win, and pick your track — about 10 minutes."
  />
  <LinkCard
    title="What is DotBoxD? (choosing a mode)"
    href="/overview/"
    description="The mental model — one contract, three modes, one sandbox — and a decision table."
  />
  <LinkCard
    title="Tutorials"
    href="/tutorials/"
    description="Three end-to-end tracks — call, react, extend — plus an advanced hand-written IR path."
  />
  <LinkCard
    title="Example: GameServer"
    href="/examples/gameserver-walkthrough/"
    description="An annotated tour of the maintained sample that ties all three modes together."
  />
  <LinkCard
    title="Security model"
    href="/security/sandbox-caveats/"
    description="What is and isn't a boundary — read before deploying untrusted plugins."
  />
  <LinkCard
    title="API reference"
    href="/api/"
    description="Generated from the source of every published package."
  />
</CardGrid>

---

DotBoxD merges the former standalone **ShaRPC** (RPC) and **Safe-IR** (kernel sandbox) projects into
one contract-first runtime. It is [MIT licensed](https://github.com/JKamsker/DotBoxD/blob/main/LICENSE).
